# ThriveLife — environment template
# Copy to .env (or apps/web/.env.local) and fill in values. Never commit real secrets.

# NODE_ENV=development
# NEXT_PUBLIC_APP_URL=http://localhost:3000

# Auth — Clerk planned for Phase 9 (local scaffold uses stub session + tl_dev_role cookie)
# NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
# CLERK_SECRET_KEY=

# Database — Postgres when Phase 2 schema lands (prefer Canada-region host)
# DATABASE_URL=

# Optional later
# AUTH_SECRET=
# SENTRY_DSN=
