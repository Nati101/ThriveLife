# Deprecated placeholder
#
# Content editor / admin tools live in the **same web app** via role-gated
# routes under `apps/web` (`/admin/*`). Do not build a separate admin deploy.
#
# Kept so older docs/links that mention `/admin` at repo root stay clear.
