# Deprecated layout note: content editing is role-gated inside the same web app
# (see docs/TASKS.md Phase 0.2 / 2.3). This folder is a placeholder only; do not
# build a separate admin deploy here unless requirements change.
